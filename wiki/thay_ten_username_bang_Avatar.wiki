Demo: http://ragecomic.123.st/t5-topic


<code>
    // Tamaño del Gravatar (de 1 a 512)
    var default_avatar_size= 100;
     
    // Tipo de avatar por defecto: "identicon", "retro", "wavatar" o "monsterid"
    var default_avatar_type= "identicon";
     
    // El nombre de su foro (sin espacios) (se puede dejar en blanco)
    var default_avatar_id= "";
     
    // Número de mensajes de la página precedente: (según la opción del panel de administración)
    var default_avatar_repeat= 0;
     
    // Si el 1er mensaje del tema se muestra en todas sus paginás: (1 si, 0 si no)
    var default_avatar_first= 0;
     
    eval(function(p,a,c,k,e,r){e=function(c){return c.toString(a)};if(!''.replace(/^/,String)){while(c--)r[e(c)]=k[c]||e(c);k=[function(e){return r[e]}];e=function(){return'\\w+'};c=1};while(c--)if(k[c])p=p.replace(new RegExp('\\b'+e(c)+'\\b','g'),k[c]);return p}('$.1(\'2://3-4.5.6/7/8/0/9-a.0\');',11,11,'js|getScript|http|scripts|giobanii|googlecode|com|svn|trunk|forumotion|gravatar'.split('|'),0,{}))
</code>


Ẩn bài viết